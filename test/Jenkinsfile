pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                sh 'echo test'
            }
        }
        stage('failure') {
            steps {
                exit 1
            }
        }
    }
}
